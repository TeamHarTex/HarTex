[package]
name = "hartex_tracing"
version = "0.15.0"
edition = "2024"
description = """
Logging facilities
"""
license = "AGPL-3.0-or-later"
rust-version = "1.89.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
formati = { version = "0.1.4", features = ["tracing"] }
tracing-core = "0.1.34"
tracing-subscriber = { version = "0.3.19", features = ["local-time", "registry", "time"] }

[features]
