[package]
name = "hartex_log"
version = "0.15.0"
edition = "2024"
description = """
Logging facilities
"""
license = "AGPL-3.0-or-later"
rust-version = "1.89.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
formati = { version = "0.1.2", features = ["log"] }
log = "0.4.27"
log4rs = { git = "https://github.com/TeamHarTex/log4rs.git", branch = "master", default-features = false, features = [
    "console_appender",
] }

[features]
