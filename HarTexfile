project discord-frontend {
    type = "rust"

    profile = "release"
    include-debug-info = true
}

project hartexbuild {
    type = "rust"

    profile = "release"
    include-debug-info = true
}

project localization {
    type = "rust"

    profile = "release"
    include-debug-info = true
}

project rust-utilities {
    type = "rust"

    profile = "release"
    include-debug-info = true
}

project web-frontend {
    type = "jsts"

    package-manager = "yarn"
}
