name: Tests

on:
  push:
    branches:
      - nightly
      - stable
  pull_request:
    branches:
      - "**"

env:
  CARGO_TERM_COLOR: always

jobs:
  Linux:
    name: Linux (Full Build)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install latest nightly Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          override: true
          profile: default
      - name: Obtain Default Build Configuration
        run: curl https://gist.githubusercontent.com/HTG-YT/9f0786e09b7fb7be1e8ea6b50dbeaee2/raw/a3b5c67d3fddc8a3e3be6861995b3ad098d04d83/buildconf.toml > buildconf.toml
      - name: Build
        run: cargo build --release
        env:
          ENABLE_PGSQL_CACHE_BACKEND: false

  macOS:
    name: macOS (Full Build)
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install latest nightly Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          override: true
          profile: default
      - name: Build
        run: cargo build --release
        env:
          ENABLE_PGSQL_CACHE_BACKEND: false

  Windows:
    name: Windows (Full Build)
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install latest nightly Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          override: true
          profile: default
      - name: Build
        run: cargo build --release
        env:
          ENABLE_PGSQL_CACHE_BACKEND: false

  LinuxTest:
    name: Linux (Test)
    needs:
      - Linux
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install latest nightly Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          override: true
          profile: default
      - name: Test
        run: cargo test
        env:
          ENABLE_PGSQL_CACHE_BACKEND: false

  macOSTest:
    name: macOS (Test)
    needs:
      - macOS
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install latest nightly Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          override: true
          profile: default
      - name: Test
        run: cargo test
        env:
          ENABLE_PGSQL_CACHE_BACKEND: false

  WindowsTest:
    name: Windows (Test)
    needs:
      - Windows
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install latest nightly Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          override: true
          profile: default
      - name: Test
        run: cargo test
        env:
          ENABLE_PGSQL_CACHE_BACKEND: false
