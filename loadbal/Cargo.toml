[package]
name = "loadbal"
version = "0.1.0"
edition = "2021"
description = """
Load balancer implementation.
"""
license = "AGPL-3.0-only"
rust-version = "1.62.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
base = { path = "../base", features = ["cmdline", "logging", "panicking"] }
env = { path = "../env" }

dashmap = "5.2.0"
futures-util = "0.3.21"
hyper = { version = "0.14.17", features = ["client", "http1"], default-features = false }
log = "0.4.16"
serde = { version = "1.0.136", features = ["derive", "std"], default-features = false }
serde_json = "1.0.79"
tide = "0.17.0-beta.1"
tokio = { version = "1.17.0", features = ["macros", "net", "rt", "rt-multi-thread", "sync"] }

[build-dependencies]
toml_edit = "0.14.2"

[[bin]]
name = "loadbal"
path = "bin/main.rs"

[lib]
name = "loadbal"
crate-type = ["rlib"]
path = "lib/lib.rs"
